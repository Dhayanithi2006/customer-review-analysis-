You are a Principal Product Analyst.

Analyze the following {count} customer reviews and categorize each review according to the strict guidelines below.

FIXED CATEGORIES (Choose EXACTLY one):
Bug | Performance | UX | Pricing | Feature Request | Onboarding | Customer Support | Data & Privacy | Integration | Praise

FIXED BUSINESS AREAS (Choose EXACTLY one):
Checkout | Auth | Core Feature | UI | Billing | API | Other

NEGATIVE CONSTRAINTS (ANTI-HALLUCINATION RULES):
1. DO NOT calculate percentages, counts, totals, or priority scores.
2. DO NOT invent categories outside the fixed lists provided above.
3. DO NOT output conversational text, markdown headers, or explanations.
4. Return ONLY a valid JSON array starting with '[' and ending with ']'.
5. Base severity strictly on user impact: 10=critical data loss or revenue loss, 1=minor cosmetic.
6. Extract a confidence score (0-100) reflecting certainty in categorization. If unclear, set confidence < 50.

OUTPUT FORMAT (JSON ARRAY ONLY):
[
  {{
    "review_index": <integer index matching input>,
    "issue_key": "<SCREAMING_SNAKE_CASE, max 4 words, specific e.g. PAYMENT_RETRY_FAILED>",
    "category": "<One of the Fixed Categories>",
    "severity": <integer 1 to 10>,
    "confidence": <integer 0 to 100>,
    "summary": "<one sentence, max 15 words>",
    "business_area": "<One of the Fixed Business Areas>"
  }}
]

REVIEWS TO ANALYZE:
{reviews_json}
